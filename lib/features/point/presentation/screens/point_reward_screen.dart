import 'package:flutter/material.dart';

class PointRewardScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('기프티콘')),
      body: Center(child: const Text('받은 기프티콘 목록')),
    );
  }
}
